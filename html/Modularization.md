modularization from HTML and CSS 
---------------------------------
all html tags and css selectors where they can be divided into modules, so creating modulated blocks is much easier to work with in html and css.